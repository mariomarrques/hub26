// This page is no longer used - products are now in a unified listing at /produtos
import { Navigate } from "react-router-dom";

const Categoria = () => <Navigate to="/produtos" replace />;

export default Categoria;
